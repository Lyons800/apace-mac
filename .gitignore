# Xcode / Swift
xcuserdata/
*.hmap
*.dSYM.zip
*.dSYM
timeline.xctimeline
playground.xcworkspace
DerivedData/
build/
*.xcresult

# Swift Package Manager
.build/
# Package.resolved is committed on purpose (deps are pinned for an app).

# Generated by XcodeGen from project.yml — never committed (see ADR-0006).
*.xcodeproj

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns

# Signing material & local secrets — must never land in a public repo
*.p12
*.cer
*.mobileprovision
*.xcconfig.local
ExportOptions.local.plist

# Private strategy / competitive notes — kept out of the public repo
/notes/
